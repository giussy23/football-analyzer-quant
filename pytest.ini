[pytest]
# Permite ejecutar simplemente `pytest` desde D:\football_analyzer sin tener
# que exportar PYTHONPATH: añade el directorio padre (D:\) al sys.path para que
# `import football_analyzer...` resuelva.
pythonpath = ..
testpaths = tests
addopts = -q
